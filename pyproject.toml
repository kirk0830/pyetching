[tool.poetry]
name = "pyetching"
version = "0.0.1"
description = ""
authors = ["kirk0830 <yike.huang@aliyun.com>"]

[tool.poetry.dependencies]
python = ">=3.10"
scipy = "*"
numpy = "*"
matplotlib = "*"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.package]
include = ["pyetching/**"]